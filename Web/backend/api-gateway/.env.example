# ============================================================
# api-gateway — .env.example
# Copy thành .env và điền giá trị thực
#   cp .env.example .env
# ============================================================

# Port lắng nghe của api-gateway
PORT=4000

# Node environment
NODE_ENV=development
